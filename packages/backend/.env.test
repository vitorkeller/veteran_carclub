DATABASE_URL=postgres://veteran_carclub:veteran_carclub@localhost:5432/veteran_carclub_test
PORT=3001
JWT_SECRET=segredo-so-para-os-testes
JWT_EXPIRACAO=7d

# Fake de propósito: os testes deste momento não tocam no Storage de verdade
# (nenhum deles chama services/armazenamento.js), mas config/env.js exige
# essas variáveis pra a aplicação nem subir. Se algum teste futuro precisar
# testar upload de verdade, faça mock de services/armazenamento.js em vez de
# trocar isso por uma credencial real.
SUPABASE_URL=https://teste-fake.supabase.co
SUPABASE_SERVICE_ROLE_KEY=chave-fake-so-para-os-testes
SUPABASE_STORAGE_BUCKET=uploads

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=Testes <nao-responder@teste.local>
